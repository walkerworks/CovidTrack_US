export const localStorage = {
  CURRENT_CLIENT: 'current-client',
};

export const apiEndpoint = {
  /* Authenticate */
  SESSION: '/api/session',
  /* Update */
  SOME_CALL: '/api/getData/hi',
};

export default {
  apiEndpoint,
  localStorage,
};